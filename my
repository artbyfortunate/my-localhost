
# get {my} config
. "${HOME}/my-localhost/config.zsh"


# run {my} program
function my() {

    if [ ! -d "${HOME}/Host" ]; then

        . "${PROGRAM_HOST}/init.zsh"

    else

        . "${BASE}/run.zsh"

    fi

}